# AloCsvEditor 设计文档

- 版本：v1.8.4（第十~十五轮：注释行语义定稿 → GPL 发布 → 表头网格线 → 操作手感修复 → 快捷注释/填充/观感 → 打字进编辑与输入法 key-sink / 冻结只读 / 筛选框右键菜单）
- 日期：2026-09-14
- 状态：v1.8.0–v1.8.3 已发布（GitHub Release）；v1.8.4 已本地打包，待用户验收
- 目标用户：自己（Windows 10/11 单机使用）

---

## 1. 背景与目标

### 1.1 背景

市面 CSV 工具逐个踩坑后，决定自己做一个：

| 已试工具 | 问题 |
|---|---|
| SmoothCSV | 拖动填充报 `Invalid array length`，官方复现不了不了了之 |
| Modern CSV / Ron's Data Edit | 免费版功能阉割 / 有行数限制 |
| Tablecruncher / CSVed / CSV Buddy | 能用但界面老旧、不顺手 |
| Excel | 直接打开 CSV 会静默改数据（`00123`→`123`、长数字变科学计数法） |

### 1.2 目标

做一个 **Windows 专用、现代风格、打开即用** 的 CSV 编辑器：

1. **自动识别编码**：UTF-8（带/不带 BOM）、GBK/GB18030、UTF-16，不乱码，可手动切换。
2. **方便查看**：虚拟滚动，几万行流畅滚动；现代界面 + 暗色模式。
3. **方便编辑**：Excel 式全套编辑功能，特别是**拖动填充必须稳定可用**。
4. **不破坏数据**：原编码、原换行符回写；引号只在必要时添加。

### 1.3 非目标（v1 明确不做）

SQL 查询、数据透视表、CSV 对比、宏/脚本、多标签页、公式、跨平台、安装程序、大文件（百万行级）专项优化。

---

## 2. 需求规格

### 2.1 功能需求

| 编号 | 功能 | 说明 |
|---|---|---|
| F-01 | 打开文件 | 文件对话框 / 命令行参数（支持"打开方式"关联）/ 页面拖放（WebView2 吞 OLE，Form 层收不到，JS 读 base64 发 C#；无真实路径，保存弹框；超 50MB 拒绝；脏时先提示保存） |
| F-02 | 编码自动识别 | BOM → 严格 UTF-8 校验 → GB18030 兜底；状态栏显示，可手动切换重载 |
| F-03 | 分隔符自动识别 | `,` `;` `Tab` `\|` 打分选择；可手动切换重载 |
| F-04 | 换行符识别与保留 | CRLF / LF，保存时原样回写 |
| F-05 | 网格浏览 | 行号列 + 列头、虚拟滚动、列宽拖拽（边界左右各 12px 热区，左边缘归前一列；光标 ew-resize 设在目标格元素上，拖拽中 body.col-dragging 锁定——容器级会被子类 cursor 盖掉） |
| F-06 | 单元格编辑 | 单击选中 / 同格 400ms 内两次按下进编辑（不依赖浏览器 dblclick）/ F2 / 直接打字；Enter/Tab 提交并移动；Esc 取消；冻结行编辑框钉冻结区，隐藏行拒绝进入。**直接打字走 key-sink（见 §6.7），中文输入法从第一个键起就正常组字** |
| F-07 | 选区 | 拖选、Shift 点选/方向键扩展、Ctrl+A 全选、Ctrl+单击多选区（单格增删，活动区内忽略）、Esc 清多选区 |
| F-08 | 剪贴板 | 复制/剪切/粘贴（TSV；多区复制块间空一行；剪切/清空只作用活动区；注释行复制拷原文）；超 20 万格粘贴先确认（阈值曾漏定义致粘贴抛错，已补）；**焦点在输入控件（筛选框/查找替换框/单元格编辑框）时一律放行原生行为**——详见 §6.8 |
| F-09 | **拖动填充** | 填充柄拖拽；双击填充柄向下填到数据末尾。**单格拖动：默认纯复制，按住 Ctrl 才递增**（纯数字 +1；带尾巴数字 `A001` 按后缀递增且补零保留）；**多格拖动**：全数字等差延续 / 同前缀尾巴数字等差 / 其他循环重复 |
| F-10 | 拖动移动 | 拖选区边框移动；Ctrl+拖动=复制 |
| F-11 | 撤销/重做 | Ctrl+Z / Ctrl+Y，覆盖所有编辑类操作，上限 200 步 |
| F-12 | 行列操作 | 插入/删除行列（右键菜单 / Ctrl+I 下方插行 / Ctrl+U 上方插行 / Ctrl+J 右侧插列 / Ctrl+K 左侧插列 / Delete 整行选中删行、整列选中删列）；插入/删除单元格（shift 模式）→ v2 |
| F-13 | 排序 | 点列头 升序→降序→恢复原始顺序；数字按数值比 |
| F-14 | 筛选 | 工具条关键字过滤（全部列 / 指定列字母，大小写不敏感包含）；表头行永不隐藏；作用列下拉随列数自动同步（合法选择保留）；**筛选框支持右键菜单（复制/粘贴/删除，只动输入框不碰数据）+ 右侧 X 清除按钮（有内容才显示）** |
| F-15 | 查找替换 | Ctrl+F 查找栏：上一个/下一个、高亮、替换当前/替换全部、大小写开关、正则开关 |
| F-16 | 保存 | 原编码回写；脏标记（标题 `*`，无边框后显示在自绘标题栏）；关闭未保存真提示（C# 三选框 → saveAndClose → JS 存 → saveResult{ok} → 关；取消/失败停留；无过期标记） |
| F-17 | 另存格式 | 编码（含 BOM 开关）/ 分隔符 / 换行符 可改后保存 |
| F-18 | 右键菜单 | 复制/粘贴/插入/删除/排序/清除筛选；行列项带快捷键 hint（Ctrl+U/I/K/J/Del）；**行号菜单顶部含「注释该行 / 取消注释该行」（hint F4，文案随当前行状态切换）**；别处按下（pointerdown 捕获）/ Esc / 滚动 / 失焦即收 |
| F-19 | 主题 | 亮色 / 暗色切换（默认暗色，工具栏 + 设置面板双入口，持久化） |
| F-20 | 自检模式 | `AloCsvEditor.exe --selftest` 跑编码读写自检，输出 PASS/FAIL |
| F-21 | 新建文档 | 工具栏新建 10 行×6 列空表；未命名上下文（分隔符下拉只换上下文）；保存/另存弹对话框，按选定分隔符写；脏时新建先提示保存 |
| F-22 | 无边框窗体 | `FormBorderStyle=None`；HTML 自绘标题栏（文件名+三键，紧凑）；拖动/双击/三键走 bridge；C# `WM_NCHITTEST` 8px 四边缩放；CS_DROPSHADOW 阴影；最大化贴工作区 |
| F-22补 | 窗体缩放真相 | Form 层 `WM_NCHITTEST` 是死代码（WebView2 子 HWND 吞掉 hit-test，#446/#704）；改 JS 判定视口最外 8px（`winResize{ht}`，标题栏按钮优先）→ C# `SC_SIZE` 进 OS 原生循环（Aero 贴靠免费）；真鼠标 E2E：四边 hover 判定全对，E/S 拖分阶段快照精确跟手；首轮 E2E 曾因环境干扰出过脏数据（未置顶/失焦），教训：真鼠标测试必须置顶+前台 |
| F-23 | 设置持久化 | `%AppData%\AloCsvEditor\settings.json`；主题/内容缩放/系统字号/窗口 bounds+最大化/十字/斑马/默认分隔符/表头；旧 localStorage 一次性迁移 |
| F-24 | 十字高亮 | 活动格整行整列高亮（`--cross` 暗/亮两套），默认开，工具栏 + 设置面板开关 |
| F-25 | 斑马纹 | 奇数行交错底色（`--zebra` 暗/亮两套），默认开，工具栏 + 设置面板开关；优先级低于查找高亮 |
| F-26 | 缩放分离 | Ctrl+滚轮只缩放内容区（字号+行高 0.7–2.0，持久化）；列头/行号/工具栏/菜单跟系统字号（设置面板调） |
| F-27 | 通栏注释行 | 首列行首命中标记即成立（内容推导，不要求单字段——首列随时补标记，该行立刻变注释行）；设置可配标记（默认 `# //`）；**注释行＝整行不透明文本**：显示/编辑/复制/回写都用整行拼接文本（内部逗号不再当分隔符），编辑它=改整行文本；**去掉标记＝按分隔符重新解析回多列**；排序钉位/筛选永显；尾部空字段裁掉 |
| F-28 | 表头行指定 | 默认跳过注释首个内容行（打开重置，会话有效，不持久化）；右键行号“设为表头行”/设置面板数字框；冻结+排序排除+筛选跳过 |
| F-29 | 关于对话框 | 设置面板入口；显示版本（C# 程序集版本下发，单一真相源）、作者、开源说明、技术栈；点遮罩/Esc/关闭按钮收 |
| F-30 | **快捷注释行** | 选中**整行**（点行号）时按 **`F4`** 或 **`Ctrl+/`** 切换注释/取消注释；注释字符取设置里「注释标记」的**第一个**（可自定义）；多行选中可批量；只选中单格时不动手、只提示；输入控件内不接管 |
| F-31 | 许可与署名 | GPL-3.0（`LICENSE` 全文）+ `THIRD-PARTY-NOTICES.txt`（.NET MIT / WebView2 微软）；作者署名「阿龙」（关于框内），仓库 README 用 `Arlon` |
| F-32 | **冻结（只读）** | 工具栏右侧开关，点亮后**表格不可编辑、只能查看**（选中/复制/筛选/查找/滚动照常）。所有会改数据的入口统一过 `grid.blockEdit(toast)`：编辑(打字/F2/双击)、Delete/Backspace、粘贴/剪切、拖动填充/移动、插删行列、排序、查找替换、注释开关。**误编辑时 toast 提示先解冻**（提示节流 1.5s 防刷屏）。会话态不持久化（避免下次开程序莫名不能编辑） |

### 2.2 性能需求

| 指标 | 目标 |
|---|---|
| 打开 5 万行 × 20 列 | < 2 秒 |
| 滚动 | 无肉眼卡顿（rAF 节流渲染） |
| 单元格编辑响应 | < 50ms |
| 10 万行内存占用 | < 300MB |
| 设计上限声明 | 10 万行内流畅；更大文件允许打开（尽力模式），超出 100 万行拒绝并提示 |

### 2.3 非功能需求

- 单 exe 绿色运行（WebView2 Runtime 用系统已装的 152 版，不打包）。
- 发布：`dotnet publish AloCsvEditor -p:PublishProfile=win-x64` → `publish\AloCsvEditor.exe`
  单文件约 50MB（含 .NET 10 运行时 + WebView2 加载器 + 前端内嵌；Release + 压缩 + 无 pdb）。
  x64，Win10（21H2/1809/1607 系）/ Win11 可跑；ARM 机不行。
- WebView2 Runtime 本体不在包里（官方只支持 Evergreen 安装或 Fixed 版文件夹，Fixed 版 +100~250MB 且破单文件）：
  绝大多数 Win10/Win11 自带；缺失时启动即人话提示（exit 3），装 Edge 或搜「WebView2 Runtime」即可。
- 图标：`AloCsvEditor/app.ico`（蓝底白表格，16~256 六尺寸，GDI+ 生成），任务栏/文件图标。
- 用户数据目录：`%LocalAppData%\AloCsvEditor\WebView2`（exe 可能落在只读位置，不用默认的 exe 旁目录）。
- 设置文件固定位置 `%AppData%\AloCsvEditor\settings.json`（JSON，可手改）。
- 代码中文注释；关键位置（编码识别、解析状态机、填充范围计算、撤销栈）必须有注释。
- 交付代码可直接编译运行，无 TODO 占位。

---

## 3. 技术选型

| 选型 | 决定 | 理由 |
|---|---|---|
| 目标框架 | .NET 10（`net10.0-windows`）WinForms | 本机有 SDK 10.0.303 + VS2026；WinForms 壳代码最少 |
| 内嵌浏览器 | WebView2（NuGet 最新稳定版，**唯一引入的第三方库**） | Runtime 本机已装；HTML/CSS 做现代 UI 成本最低 |
| 前端框架 | 无（原生 HTML/CSS/JS，ES Module） | 网格性能自己可控、无构建步骤、改起来直接 |
| 前端资源 | 内嵌 EmbeddedResource（发布版单文件要求） | 调试版 Content 落盘；发布版经 WebResourceRequested 按虚拟域名 serving，不裁剪（PublishTrimmed=false，WinForms 反射经不起裁） |
| 本地资源加载 | `SetVirtualHostNameToFolderMapping` → `https://alocsv.local/` | 解决 `file://` 下 ES Module 被 CORS 拦截的问题 |
| 编码处理 | 全在 C# 侧（.NET 原生支持 GB18030） | 浏览器 `TextEncoder` 只能输出 UTF-8，写 GBK 必须走原生侧 |
| 界面 | 全部在 HTML（无原生菜单栏） | 现代外观、主题切换都在 Web 层做 |

右键菜单：关闭 WebView2 默认菜单，页面内自绘。

---

## 4. 总体架构

```
┌──────────────────────────────────────────────────────┐
│ WinForms MainForm（无边框框架：窗口动作/关闭确认/拖放/对话框/窗口记忆）│
│  ┌────────────────────────────────────────────────┐  │
│  │ WebView2 → https://alocsv.local/index.html      │  │
│  │  标题栏 / 工具栏 / 网格 / 查找栏 / 状态栏 / 设置面板 / 右键菜单 │
│  │  csv.js 解析 / grid.js 渲染 / commands 撤销      │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
         ▲ JSON 消息桥（WebMessage） ▼
┌──────────────────────────────────────────────────────┐
│ FileService：读字节→识别编码→解码→(分隔符/换行识别)       │
│              编码文本→写字节（原编码/BOM 保留）           │
│ SettingsService：settings.json 读写（窗口键 C# 自管）    │
└──────────────────────────────────────────────────────┘
```

职责划分：

| C# 侧 | JS 侧 |
|---|---|
| 无边框窗口、标题栏消息动作、四边缩放 | 全部 UI 与交互（含自绘标题栏、设置面板） |
| 读文件字节、编码识别/解码 | CSV 解析、网格渲染、编辑逻辑 |
| 按指定编码写回字节 | 序列化、选区（含多选）、填充、撤销、排序/筛选/查找 |
| 打开/保存对话框、资源管理器拖放 | 脏标记上报、主题/缩放/十字/斑马应用 |
| 命令行参数、--selftest | — |
| 设置文件读写、窗口位置记忆 | 设置镜像 + 防抖回写 |

打开数据流：用户选文件/拖放/双击 → C# 读字节 → 识别编码+解码+识别分隔符/换行 →
`fileOpened{path,text,encoding,delimiter,newline,hasBom}` → JS 解析渲染。
保存数据流：JS 序列化 → `save{text,encoding}` → C# 按编码写字节 → `fileSaved{path}`。
设置数据流：C# 启动读文件 → `ready` 后下发 `settings` 快照 → JS 应用；
JS 变更 → `saveSettings` → C# 合并落盘（窗口键 C# 自己写，互不覆盖）。

---

## 5. 目录结构

```
AloCsvEditor\
├─ DESIGN.md                        本文档
├─ AloCsvEditor.sln
├─ AloCsvEditor\                    C# 项目
│  ├─ AloCsvEditor.csproj           net10.0-windows, WinForms, WebView2 包
│  ├─ Program.cs                    入口、命令行参数、--selftest 分流
│  ├─ MainForm.cs                   窗口壳、WebView2 初始化、拖放、对话框、关闭确认
│  ├─ FileService.cs                编码识别/读写、分隔符/换行识别
│  ├─ Bridge.cs                     消息协议收发（JSON）
│  ├─ SelfTest.cs                   --selftest 自检逻辑
│  ├─ SettingsService.cs            设置持久化（%AppData% JSON，窗口键 C# 自管）
│  └─ wwwroot\                      前端资源（随 exe 发布，复制到输出目录）
│     ├─ index.html
│     ├─ css\app.css                主题变量 + 亮/暗两套
│     └─ js\
│        ├─ main.js                 启动、桥接、标题栏/工具栏/状态栏/设置面板/新建
│        ├─ settings.js             设置镜像 + 防抖回写（真相源在 C#）
│        ├─ csv.js                  解析器 + 序列化器（零 DOM 依赖，可被 Node 直接测试）
│        ├─ grid.js                 虚拟渲染 + 单/多选区 + 列宽 + 缩放行高 + 十字/斑马
│        ├─ editor.js               单元格编辑覆盖层 + 键盘语义
│        ├─ clipboard.js            复制/剪切/粘贴（TSV）
│        ├─ fill.js                 填充柄拖拽/双击填充 + 拖动移动
│        ├─ commands.js             撤销/重做栈 + 行列/排序等命令
│        └─ find.js                 查找替换栏
└─ tools\
   └─ csvtest\                      解析器 Node 单测（用例 + 大文件性能计时）
```

设计约束：`csv.js` **零 DOM 依赖**，Node 可直接 `import` 跑单测。

---

## 6. 详细设计

### 6.1 编码识别算法（FileService）

```
输入: byte[]
1. BOM 检查：
   EF BB BF → utf-8-sig；FF FE → utf-16le；FE FF → utf-16be
2. 无 BOM：用严格解码器（抛异常模式）试 UTF-8 → 成功则为 utf-8
3. 否则 → gb18030（覆盖 GBK/GB2312，中文场景兜底）
4. 空文件 → 默认 utf-8
输出: (编码名, 解码文本)
```

手动切换列表（状态栏编码指示器点击 → 重载）：`utf-8 / utf-8-sig / gb18030 / gbk / gb2312 / big5 /
utf-16le / utf-16be / windows-1252`。注：订单纯属可读性考虑，`gbk/gb2312` 是 gb18030 的子集，
放在列表里只是为了让用户"看到自己认识的名字"。

### 6.2 分隔符 / 换行识别（FileService）

- 采样前 5 个非空行，对候选 `,` `;` `\t` `|` 分别统计"引号外出现次数"与"各行列数一致性"打分，
  取最高分；并列时优先级 `,` > `;` > `\t` > `|`。
- 换行：取首次出现的换行样式（CRLF / LF / CR）；全新文件默认 CRLF。
- 识别结果显示在状态栏，可通过"另存格式"修改。

### 6.3 消息协议（JSON，双向）

C# → JS：

| type | 字段 | 说明 |
|---|---|---|
| `fileOpened` | path, fileName, text, encoding, delimiter, newline, hasBom, supportedEncodings, supportedDelimiters | 文件载入（后两个数组是 C# 的唯一真相源，JS 据此生成切换下拉框） |
| `fileSaved` | path | 保存成功 |
| `error` | message | C# 侧错误（读/写失败等） |
| `settings` | settings（存过的键）, supportedEncodings, supportedDelimiters | 设置快照（缺省 JS 用默认值补；候选数组新建文档也要用） |
| `winState` | max | 最大化状态变化（标题栏图标同步；页面未就绪时 Post 自动丢弃） |

JS → C#：

| type | 字段 | 说明 |
|---|---|---|
| `ready` | — | 页面就绪，C# 下发 settings 快照 + 启动文件 |
| `saveSettings` | settings | JS 侧设置变更（C# 合并落盘，窗口键不受影响） |
| `winDrag` / `winMin` / `winToggleMax` / `winClose` | — | 自绘标题栏窗口动作（拖动/最小化/最大化切换/关闭） |
| `winResize` | ht（10..17，同 HTLEFT..HTBOTTOMRIGHT） | 视口边缘按下，C# 进 OS 原生 sizing 循环 |
| `saveAndClose` | — | 关闭时选"是"：JS 按当前上下文保存（无路径弹另存） |
| `dropFile` | fileName, base64 | 页面拖放：字节流载入（识别管线与 Load 同一套；无盘路径） |
| `saveResult` | ok | 每次保存终态回执（成功/取消/失败都有）；JS 仅"关闭等待+ok"才回 closeReply |
| `closeReply` | ok | JS 存完，C# 继续关闭 |
| `openFileDialog` | — | 弹出打开对话框 |
| `reloadWithEncoding` | encoding, delimiter | 按指定格式重新载入当前文件 |
| `save` | text, encoding, newline, hasBom | path 已知时直接写 |
| `saveAs` | text, encoding, newline, hasBom | 先弹保存对话框再写 |
| `fileOpenedAck` | fileName, chars | 文件已接收处理确认；C# 据此更新标题（兼作桥接双向验证点） |
| `setDirty` | dirty | 上报脏标记（C# 关闭时用） |

注：窗口标题归 C# 所有（基础标题 + 脏标记 ` *`），JS 只上报 setDirty，不直接改标题。

### 6.4 CSV 解析器（csv.js，状态机，RFC 4180 + 容错）

状态：`START_FIELD / IN_FIELD / IN_QUOTED / QUOTE_END`。

- `START_FIELD`：遇 `"` → IN_QUOTED；遇分隔符 → 结束空字段；遇换行 → 结束行；其他 → IN_FIELD。
- `IN_FIELD`：分隔符/换行结束字段（行）；其他追加。
- `IN_QUOTED`：`"` → QUOTE_END；其他（含换行）追加。
- `QUOTE_END`：`"` → 追加一个 `"` 回 IN_QUOTED（转义）；分隔符/换行正常结束；**其他字符容错并入字段**，
  直到分隔符/换行（Excel 式宽容，不丢数据）。

 参差行（列数不一）：以最大列数为准，较短行补 `""`（注释行跳过补齐，保持单字段），状态栏 warning 计数显示。
 引号策略（序列化）：字段仅在含分隔符 / `"` / 换行 / 回车时加引号，内部 `"` 双写；
 注释行原样回写（不包引号，重解析行首命中仍是单字段，roundtrip 稳定）。
首尾空格**不**加引号（最小化 diff，Excel 行为一致）。

### 6.5 虚拟网格（grid.js）

- **key-sink（输入法靶子，v1.8.4）**：一个隐形但**可聚焦**的 textarea，网格有焦点时常驻焦点。
  为什么必须有：**输入法只对"当前已聚焦的可编辑元素"生效**。焦点若落在不可编辑的 scroller 上，
  用户按下的第一个拼音字母会被输入法当普通按键放过去（形成孤立英文字母），后面的字母才开始组字。
  做法：`scroller` 的 `focus` 事件把焦点重定向到 key-sink（一处顶掉全部 `scroller.focus()` 调用点）；
  key-sink 的 `input`（`isComposing` 为假时）/ `compositionend` 取走已产生的文本作为编辑初值。
  两个必须注意的坑：
  1. **不能用 `display:none` / `visibility:hidden` 藏它**——那样元素拿不到焦点；必须 1px + `opacity:0`。
  2. key-sink 是 TEXTAREA，会被"输入控件放行"的守卫误判 → 必须靠 `data-keysink` 排除，
     否则剪贴板、Ctrl+F、F4 等全局快捷键会失效（判定集中在 dom.js，见 §6.15）。

- 基准行高 28px × zoom（0.7–2.0，Ctrl+滚轮 / 设置面板，持久化）；实例行高走 `rh()`，
  CSS `--row-h` 同步；列头 30px、行号宽 **53px**（原 48px，v1.8.4 加宽以容纳 5 位行号；字号跟系统）。
  **`ROW_NUM_W`（grid.js）与 CSS `.grc` / `.grid-corner` 必须同步改**，否则点击定位错位。
- 布局：左上拐角 + 列头行 + 行号列 + 滚动体。列头/行号用 sticky 定位。
- 渲染窗口 = 可视行 ± 20 行 overscan，`scroll` 事件 rAF 节流后重排；行 div 绝对定位，
  `top = rowIndex × 28`；容器内垫片 div 高度 = 总行数 × 28 撑起滚动条。
- 列：v1 全量渲染单元格（几十列无压力）；列数 > 200 时状态栏提示可能变慢（v2 做列虚拟化）。
- 文本溢出省略号；进入编辑时覆盖层自适应高度（textarea）。

### 6.6 选区模型

活动区 `{ar,ac,fr,fc}`（anchor+focus）+ 多选区 `extra[]`（规范化矩形列表）。
编辑/填充/删除/排序/清空/粘贴只作用活动区；复制/剪切拷贝全部选区（块间空一行）。
操作：拖选、Shift+点选/方向键扩展、Ctrl+A 全选、列头点选整列、行号点选整行、
Ctrl+单击增删单格多选区（活动区内忽略，矩形无洞）、Esc 清多选区。
渲染：活动区覆盖层 + `.grid-range-extra` 虚线覆盖层（body/冻结段各一套）。

### 6.7 单元格编辑（editor.js）

- 同格 400ms 内两次按下 / F2 / 直接打字进入编辑（打字=替换模式；
  不依赖浏览器 `dblclick` 事件，原生 `dblclick` 仅作兜底）；Enter 提交并下移，Shift+Enter 上移，
  Tab/Shift+Tab 左右，Esc 取消。
- **打字/输入法的入口是 key-sink，不是 keydown**（见 §6.5）：字符先落到 key-sink，
  由它的 `input` / `compositionend` 取到整段文本再作为初值开编辑。这样中文输入法从第一个键起就组字。
  编辑器光标位置分两种：**打字进来（initial 非空）→ 光标落末尾**（后续字符追加）；
  **F2/双击（initial 为空）→ 保留原值并全选**（方便直接覆盖）。
- 编辑框跟视觉位置：冻结行钉在冻结区（滚动不提交），筛选隐藏行拒绝进入。
- 编辑覆盖层为 textarea，定位到单元格，Enter 换行用 Alt+Enter（与 v3.24 一致习惯）。
- 每次提交 = 1 条撤销记录。

### 6.8 剪贴板（clipboard.js，TSV 与 Excel 互通）

- 复制/剪切：全部选区 → TSV（多区之间空一行；剪切只清空活动区；含换行/制表符的字段加引号——Excel 兼容规则）。
- 粘贴：解析剪贴板 TSV，从活动单元格开始写入，自动扩展行列；超 20 万格时先确认。
- 超大阈值 `BIG_PASTE_CELLS = 20 万`（曾漏定义导致粘贴监听直接抛错，多选改造时顺手修复；
  纯函数单测覆盖不到事件监听层，靠 headless 合成事件验证）。
- **输入控件放行（v1.8.3 修复）**：`copy/cut/paste` 挂在 `document` 上，放行条件必须覆盖
  **一切可编辑控件**（`INPUT`/`TEXTAREA`/`contentEditable`），不能只判断「单元格编辑框是否打开」。
  否则在筛选框/查找框里按 Ctrl+V 会被网格接管——**内容被当 TSV 写进单元格**（污染数据、标脏文件），
  Ctrl+C 会拷成表格内容、Ctrl+X 还会清空网格选区。判据：事件目标为主，当前焦点元素兜底。
- 实现：`copy/cut/paste` 事件 + `clipboardData`，并备 `execCommand` 回退。

### 6.9 拖动填充（fill.js，硬需求，重点防错）

- 填充柄：选区右下角 8px 方块；按下拖拽 → 预览虚线框（实时钳制在合法范围内）→ 松开应用。
- 目标范围计算**必须钳制**：`count = max(0, …)`，逐格写入，**禁止用推导出的长度去 `new Array(n)`**
 （这正是 SmoothCSV `Invalid array length` 一类 bug 的根因，本项目以规范杜绝）。
- 填充规则（沿拖拽方向）：
  1. **源 1 格：默认纯复制（不做任何增量）**；按住 Ctrl 才递增——纯数字 +1 步进，
     尾巴数字（`A001`/`第3周`）按后缀递增且补零位宽保留；
  2. 源 ≥2 格且全为数字、步长恒定 → 等差延续；
  3. 源 ≥2 格全尾巴数字且前缀相同 → 后缀等差；
  4. 其他 → 源模式循环重复。
  （v1.8.4 变更：原来单格尾巴数字**默认就递增**，现改为需按 Ctrl；多格行为未动。
   实现要点：`fillValueAt()` 的单值分支先判 `forceSeq`，未按 Ctrl 直接返回原值。）
- 双击填充柄：沿"左/右邻列有数据的连续区间"向下填充（Excel 行为）。
- 拖出当前末尾自动扩展行（单次上限过程中累计不超过 100 万行，超了弹 toast 拒绝）。
- 一次填充 = 1 条撤销记录。

### 6.10 拖动移动（fill.js）

- 按住选区边框拖拽 → 移动内容（源区清空）；Ctrl+拖拽 → 复制。
- 目标越界钳制；一次移动 = 1 条撤销记录。

### 6.11 撤销/重做（commands.js，命令模式）

- 命令存 diff：单元格 `{r,c,before,after}[]`；插/删行列存 `{index, rows/cols}` 快照；
  排序存行索引置换。
- 上限 200 步，超了丢最旧；单命令 diff 超 20 万格时 toast 提示且仍执行（内存可接受）。

### 6.12 行列 / 排序 / 筛选 / 查找

- 行列：右键菜单（插入上/下/左/右、删除行/列）；插入/删除单元格（shift 模式）→ v2。
- 右键菜单收尾：`pointerdown` 捕获 + Esc + 滚动 + `window blur`
  （`document click` 捕获在部分场景收不到，`pointerdown` 一定先到；点窗体外只能靠 `blur`）。
- 排序：右键列头菜单（升序/降序/清除排序）+ 列头 ▲▼ 指示；两值皆数字按数值比；
  表头模式下表头行之前不动，注释行钉原位（sortWithComments，只排数据行）；
  排序打乱后可用"清除排序"恢复原始顺序（F-12 相关 M9 实现）。
- 筛选：工具条关键字（150ms 防抖）+ 作用域（全部列/指定列字母，列数变化自动同步）；大小写不敏感包含匹配；
  表头之前（含表头）跳过，注释行永不隐藏；**筛选状态下禁止结构性修改**（插删行列/排序），toast 提示先清除筛选；
  单元格编辑/填充/粘贴按绝对行列作用（含隐藏行，连续语义）；查找限定可见行。
- 右键行号“设为表头行”（当前打勾，会话有效，打开新文件重置为首个内容行）。
- 查找替换栏（Ctrl+F）：上一个/下一个、结果计数、可视区高亮、替换当前/全部、
  大小写开关、正则开关；数据修改后高亮自动清除（查询文本保留）。

### 6.13 主题与样式

CSS 变量两套主题，默认暗色，工具栏一键切换；字体 `Segoe UI Variable / 微软雅黑`；
圆角 8px、1px 分隔线、选中高亮蓝、填充柄深蓝方块（Excel 既视感）。
十字高亮 `--cross` / 斑马纹 `--zebra` 各暗/亮两套；数据表头行 `--fheader` + accent 下边线，
与 ABCD/行号（`--bg-header`）拉开；`:where()` 压优先级保证查找高亮永远在上；
系统字号 `--sys-fs` / 内容字号 `--content-fs` / 行高 `--row-h`；
自绘标题栏 34px（图标+全路径文件名+三键，无应用名，最大化图标随 `body.maxed` 切换，× 红底白字）；
工具栏图标化（组背景透明+描边分组，格式进弹出框）；无边框窗体 1px 描边（`--win-border`，最大化隐藏）；
溢出单元格自绘 tooltip（跟系统字体，1px 遮挡也提示，原生 title 字体不可控）。
通栏注释行 `.comment-row`：整行文本单 div 占满（斜体 + `--fg-faint` + 字号 -1px）。

后续几轮的观感/手感项（v1.8.2~v1.8.4）：

- **表头网格线 `--head-line`**（v1.8.2）：表头底色 `--bg-header` 与通用线色 `--border` 色差过小
  （暗色仅 2/255、亮色 4/255），ABCD/行号之间的分隔线肉眼不可见。表头**单独取值**
  （暗 `#4d4d4d` / 亮 `#b4b4b4`，对比度提升 9~9.5 倍）；数据区仍用 `--border`，未动。
- **行号列宽 53px**（v1.8.4，原 48px）：`ROW_NUM_W`（grid.js）与 CSS `.grc` / `.grid-corner`
  **必须同步修改**，否则点击定位/编辑框位置全部错位。
- **滚动条**（v1.8.3~v1.8.4）：轨道 24px、可见滑块 12px（`::-webkit-scrollbar-thumb` 用 6px `--bg`
  边框制造内缩的"悬浮胶囊"）；再加 `min-height/min-width: 60px` 给滑块长度兜底——不加时
  20 万行下**可见滑块只剩约 5px**（横轴 2.5px），根本抓不住。
- **标题栏 `--titlebar`**（v1.8.4）：换成现代中性色（暗 `#24292f` 石板色 / 亮 `#eef1f5` 冷调浅灰），
  弃用早期偏绿的配色。
- **行号格高亮的不透明底**（v1.8.4）：`.grc.row-active` / `.grc.cross-row` 必须保留
  `background-color: var(--bg-header)` 打底，半透明高亮只作为 `background-image` 叠加——原因见 §6.15。

### 6.14 快捷键表（v1）

Ctrl+S 保存 / Ctrl+Shift+S 另存 / Ctrl+Z…Ctrl+Y / Ctrl+C/X/V /
Ctrl+F 查找 / Ctrl+H 替换 / Ctrl+A 全选 / F2 编辑 / Enter/Tab 移动 /
Delete 智能删除（整行/列选中删行列，其余清内容）/ Backspace 清空 / Shift+方向键扩展 / Ctrl+Home/End 跳转 /
Ctrl+I 下方插行 / Ctrl+U 上方插行 / Ctrl+J 右侧插列 / Ctrl+K 左侧插列 /
Delete 整行选中删行、整列选中删列（全选/普通区域只清内容） /
（注：之前写的 Ctrl++/- 从未实现，已替换为真实快捷键） /
Ctrl+单击多选区 / Esc 上下文关闭（面板/菜单/查找栏/多选区） /
**F4 或 Ctrl+/ 整行注释开关**（需先选中整行；注释字符取设置里的第一个）。

### 6.15 CSS / 前端易错点（已踩过的坑，动样式或全局按键处理前先读）

- **sticky 元素 + 半透明底 = 内容穿透**。行号格 `.grc` 是 `position: sticky; left: 0`，横向滚动时
  单元格从它**底下穿过**，而它自己的 `background: var(--bg-header)` 就是唯一的不透明底。若把
  `background` 整个换成半透明高亮（`--selection` / `--cross` 都是 rgba），底下内容就会**透出来**，
  看起来像"内容遮挡了行号"。正确写法：`background-color` 打底 + `background-image` 叠半透明层。
  列头没这问题，是因为列头自己没有背景、靠父层 `.grid-head`（不透明）兜底——
  **"它自己是不是那层底"决定了能不能用半透明**。
- **`::-webkit-scrollbar-thumb` 的 `min-height/min-width` 是 border-box**：上下（纵轴）/左右（横轴）
  各 6px 的内缩边框**计入其中**，故 `60px` 的实际**可见**长度只有约 48px。想要"可见长度 = 粗细的 2 倍"
  得按可见值再加 12px，并且**验收要量"滑块芯色"的长度**（连外框一起量会多算那圈看不见的边框）。
- **挂在 `document` 上的键盘 / 剪贴板监听，必须放行可编辑控件**
  （`INPUT` / `TEXTAREA` / `contentEditable`；事件目标为主、当前焦点元素兜底）。
  已因此踩过三次：剪贴板被网格接管（§6.8）、Ctrl+F 在筛选框里夺焦点、以及下面的 key-sink。
- **"是不是输入控件"这个判定只准写一处**（`dom.js` 的 `isNativeInputContext`）。
  血泪教训：key-sink 引入后，这个判定散在 clipboard / main / find 三处，改了前两处漏了 find，
  结果 Ctrl+F 被守卫提前 `return` 掉（没 preventDefault）→ 直接落到 WebView2 的默认查找
  （用户看到的是"系统搜索框"）。**判定集中 + key-sink 用 `data-keysink` 排除**，才不会漏。
- **量截图像素先定标 DPR**（本机实测 1.167~1.2）：用已知尺寸（如滑块可见宽 12px）反推缩放比，
  再换算 CSS px，否则数字会差一成。

---

## 7. 性能设计

- 解析：单遍状态机；5 万行×20 列目标 < 1s（tools 下 Node 计时验证）。
- 渲染：可视区 ~40 行 + overscan，DOM 节点数恒定，与总行数无关。
- 排序 10 万行：JS 原生 sort < 300ms，可接受（转圈提示）。
- 内存：数据模型 `string[][]`，10 万行×20 列约 200 万格，按平均 15 字符 ≈ 100MB 以内，
  目标 < 300MB。

---

## 8. 测试计划

### 8.1 解析器 Node 单测（tools/csvtest，可 `node` 直接跑）

| 用例 | 覆盖 |
|---|---|
| 基本逗号分隔 | 正常解析 |
| 引号内逗号/换行/转义引号 | RFC 4180 核心 |
| 空字段、空行、末尾无换行 | 边界 |
| 参差行（长/短） | 容错 + 补空 |
| 裸引号（字段中间的 `"`） | 容错不丢数据 |
| parse→serialize→parse roundtrip | 序列化稳定性 |
| 5 万行生成文件 | 解析+序列化计时（性能基线） |
| 多区复制 rangesToTsv（T45） | 块间空行；单区退化一致 |
| 单格复制 / Ctrl 递增（T46/T47） | 单格拖动**默认纯复制不增量**；按 Ctrl 才递增（纯数字 +1、`A001`→`A002` 补零保留、`第3`→`第5`）；多格等差/循环不受影响 |
| 注释行解析/序列化/锚定排序/实时判定/整行文本/注释开关（T48–T53） | 行首整行收单字段；原样回写 roundtrip；注释钉位数据照排；首列补标记也算注释；整行拼接不拆分隔符、去标记即恢复多列；`toggleCommentText` 注释用第一个前缀、取消时去掉命中的那个、支持自定义字符 |

### 8.2 C# 自检（`--selftest`）

T1 空文件→默认 utf-8；T2 UTF-8 中文识别；T3 GBK（`"中文,测试"` 字节）识别；
T4 UTF-16LE BOM；T5 gb18030 写→读 roundtrip 字节一致。输出 PASS/FAIL，exit code 表达结果。

### 8.3 真机冒烟测试文件（脚本生成放系统临时目录，不入库）

UTF-8 无 BOM / UTF-8 BOM / GBK / UTF-16LE / 引号换行混合 / 5 万行大文件。
逐个打开→编辑→填充→保存→字节比对（除有意修改外一致）。

headless 验证铁律（M9c 血泪教训）：`ExecuteScriptAsync` 不等待 Promise
（已决议的返回 `{}`，未决议的直接 hang 住 C#  await，标题永远不更新）。
诊断脚本必须同步返回；异步等待一律放 C# 侧（`Task.Delay` 分步执行）。
凡遇到"标题停住不动"，先怀疑诊断脚手架，再怀疑产品代码。

二批 headless E2E（临时 C# 驱动 + 合成事件，验后删除）：标题栏存在、新建 10×6
（rendered=9/cells=60，9 体行+6 冻结格手算一致）、主题翻转再恢复后文件内容一致、
十字 15 格/斑马 30 格（9 行小表手算一致）、Ctrl+单击多选=2/Esc 清零、滚轮 zoom=1.1 且隔夜持久、
菜单开 1/关 0、双击开编辑框（top 跟 zoom 行高一致）。
教训：合成 `mousedown` 必须每次重查节点——`render()` 重建行，旧引用变游离节点，事件冒不上去。
教训二：`cursor: inherit` 只取直接父级计算值——中间层全是 `auto`（cursor 默认不继承），
想全树覆盖必须逐元素给显式值（`body[data-edge="E"] *` 之类）；hover 光标必须读 computed style 验证，
只验 dataset 状态是半截子工程。
教训三：无坐标合成 `mousedown`（clientX/Y=0）会被边缘判定吃掉——测试用合成事件一律带真实坐标；
产品侧加 `isTrusted` 门，只认真实手势进 modal 循环。
增列 E2E：Ctrl+J 后筛选下拉 3→4 且选择保留（`SCOPE="3->4 val=0"`）。
拖放 E2E：合成 drop（真 File）→ 行列文件名全对（`rows=2/cols=2/file=drop.csv`）；
教训：页面不接 drop 的话 Chromium 直接导航到文件把应用换掉，dragover/drop 必须 preventDefault。
注释行 E2E：2 通栏 + 自动表头第 3 行 + 通栏 240px（列 120px）+ 右键设表头生效（`headerRow=4`）；
教训：右键菜单 kind 按事件 target 判定，合成 contextmenu 必须 dispatch 到目标元素上（dispatch 到 scroller 永远是 cell 菜单）。

### 8.4 用户验收

你上手实际用：打开自己的 CSV → 改 → 填充 → 保存 → 用 Excel/记事本确认结果正确。

---

## 9. 边缘用例清单（实现时逐项过）

空文件 / 仅表头 / 单列 / 单行 / 超大单格（>1MB 文本）/ 全空行 /
分隔符出现在引号内 / 换行出现在引号内 / 文件末尾无换行 / 混合换行符 /
GBK 文件含生僻字 / UTF-8 文件被误判（手动切换可救）/ 粘贴超大区域 /
填充拖到百万行外 / 排序含空值 / 筛选后删除行（禁止，见 §6.12）。

---

## 10. 实现步骤与里程碑

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| M1 | 项目骨架 + 窗口壳 + 页面加载 | `dotnet build` 零错误零警告；窗口打开显示页面 |
| M2 | C# 文件服务（编码/分隔符/换行识别、读写、对话框、拖放、命令行） | `--selftest` 全 PASS；冒烟文件能打开 |
| M3 | 消息桥 + 应用框架（工具栏/状态栏/主题/打开保存闭环） | 命令行打开→标题回执（桥接双向验证）→状态栏文件信息→原文保存字节一致 |
| M4 | CSV 解析器/序列化器 + Node 单测 | §8.1 全部通过 |
| M5 | 虚拟网格 + 选区 | 5 万行滚动流畅；选区操作全对 |
| M6 | 单元格编辑 + 剪贴板 | Excel 双向粘贴正确 |
| M7 | 撤销/重做 | 所有编辑操作可撤销 |
| M8 | 拖动填充 + 拖动移动 | §9 相关用例全过，尤其超界/空选区不报错 |
| M9 | 行列/排序/筛选/查找替换 | 功能清单全对 |
| M10 | 打磨 + 性能实测 + 修复 | §2.2 达标；§8.3 全过；你验收通过 |
| M11 | 十项二批（新建/无边框/多选/十字/斑马/缩放/设置持久化/热区/双击/菜单收尾） | §8.1 T45 + §8.2 + §8.3 二批项全过；你验收通过 |
| M12 | 通栏注释行 + 可配表头行 | §8.1 T48–T50 + §8.3 注释 E2E 全过；你验收通过 |
| M13 | 关于对话框 + 注释行实时判定/更淡更小 | §8.1 T51 + §8.3 关于/实时注释 E2E 全过；你验收通过 |
| M14 | 发布与许可（GPL-3.0 + 署名 + 第三方声明）+ 表头网格线 `--head-line` | v1.8.1 / v1.8.2 已发 Release（附件 SHA256 与本地一致）；表头线逐像素实测确认可见 |
| M15 | 操作手感修复（剪贴板输入框放行 / 滚动条加宽 / 行号格穿透 / 滑块最短长度）+ 快捷注释行 / 填充规则 / 行号列加宽 / 标题栏配色 | v1.8.3 已发 Release；本轮 §8.1 全过 + 真实程序逐像素实测；你手动验收通过 |
| M16 | 打字进编辑与输入法（key-sink）+ 冻结只读 + 行号右键注释 + 筛选框右键菜单/X 按钮 | §8.1 全过；CDP 模拟输入法组字（`n`→`ni`→提交「你」）取到完整文本；真实 Ctrl+F 实测弹出应用查找栏；你手动验收通过 |

每步做完即验证再进下一步；任何一步发现设计要改，先更新本文档。

---

## 11. 风险与对策

| 风险 | 对策 |
|---|---|
| WebView2 剪贴板在某些环境受限 | `paste` 事件 + `execCommand` 双路径回退 |
| GB18030 解码器对坏字节过于宽容导致误判 | 严格 UTF-8 优先 + 状态栏手动切换兜底 |
| 超大文件内存爆炸 | 100 万行硬上限 + 打开前按文件大小提示 |
| 填充/粘贴的超大操作卡 UI | 超 10 万格先确认；操作本身是同步循环，保持简单可预测 |
| 需求蔓延（"再加个 SQL 吧"） | 一律记 v2，v1 只修 bug 不加功能 |
| 无边框坑（Alt+空格菜单丢失/顶部缩放与拖动分区/多显示器最大化盖任务栏） | 三键+双击标题栏代替系统菜单；视口最外 6px 归缩放、其余归拖动（JS 判定）；ToggleMax 时重取 WorkingArea |

---

## 12. v2 候选（v1 不做，仅记录）

多标签页、列虚拟化（>200 列）、单行行高可调（统一缩放已做）、公式、
多重选区编辑（选择+复制已做）、CSV 对比、
SQL 控制台、一键文件关联（注册表）、安装程序、macOS/Linux、
插入/删除单元格（shift 模式）、多行冻结/冻结列。

---

## 13. 编码规范

- 注释中文；公开 API、编码识别、解析状态机、填充范围计算、撤销栈必须有注释。
- 与仓库现存风格一致（新仓库用 .NET 默认模板风格）。
- 交付单一可编译版本，无 TODO 占位；修无关代码只提不改。

---

## 14. 待确认事项

- [x] 默认主题：暗色，工具栏一键切换亮色/暗色（已确认）。
- [x] 本文档已确认，从 M1 开始实现。
- [x] 十项二批已确认并实现（分隔符用工具栏下拉/紧凑标题栏/设置放 %AppData%/列头跟系统/多选做选择+复制/十字斑马默认开），待验收。
- [x] 第三轮已实现（斑马表头加深/工具栏图标化+格式框/标题全路径+红底X/Ctrl+I/J/光标真修/缩放8px+按钮优先），待验收（#2 用户侧仍报不行：机制已多边实测无误，待用户确认最大化状态/包新鲜度）。
- [x] 第四轮已实现（关闭真提示/Delete删行列/菜单hint/U/K/填充序列+尾巴数字/列头行号hint/溢出tooltip/光标inherit教训），待验收（同上 #2 待用户确认）。
- [x] 第五轮已实现（筛选列同步/工具栏减淡/窗体描边/tooltip零阈值/去标题文字），待验收。
- [x] 第六轮拖拽打开已实现（页面接管+字节流+无路径保存语义），待验收。
- [x] 第七轮发布版已出（`publish\AloCsvEditor.exe` 单文件 50MB，内嵌资源实测可用），待验收。
- [x] 第八轮注释行+可配表头已实现（解析器行首整收/通栏渲染/排序锚定/右键指定/设置面板），T48–T50 + E2E 全过，待验收。
- [x] 第九轮关于对话框 + 首列补标记即时成注释行已实现（T51 + E2E：`open=true ver=v1.8.0 arlon=true oss=true`、`before=0 after=1 [#Alice]`），待验收。
- [x] 第十轮注释行语义定稿：整行不透明文本（编辑整行、去标记即恢复多列），更淡更小。E2E：`had=Alice comments=[#Alice,30,Beijing]` → `had=#Alice,30,Beijing cols=Alice,30,Beijing,Bob,25,Shanghai`。已随 v1.8.0 发布。
- [x] 第十一轮 发布与许可：GPL-3.0 官方全文 + README 许可证章节（人话版：可免费使用/修改/分发/PR，禁闭源收费）+ `THIRD-PARTY-NOTICES.txt`；关于框署名「阿龙」。**v1.8.1 已发 Release**。
- [x] 第十二轮 表头网格线修复：新增 `--head-line`（暗 `#4d4d4d` / 亮 `#b4b4b4`）。逐像素实测：表头带内 5 条竖线、间距均匀 144px、旧线色 0 残留；对比度提升 9~9.5 倍。**v1.8.2 已发 Release**。
- [x] 第十三轮 操作手感四修：①剪贴板输入控件放行（真实模块 18 项回归全绿 + 旧守卫对照实验复现"粘贴写进网格/剪切清空网格"10 项失败）②滚动条 12→24px ③行号格不透明底（受控红块穿透实验：红色透出 96.3% → 0%）④滑块最短长度（真实可见 5px → 48px）。**v1.8.3 已发 Release**。
- [x] 第十四轮 四项：① `F4` / `Ctrl+/` 整行注释开关（注释字符取设置第一个，可自定义）② 单格拖动填充改为**默认纯复制、按 Ctrl 才递增**（多格未动）③ 行号列 48→53px ④ 标题栏改现代中性色（暗 `#24292f` / 亮 `#eef1f5`）。新增 T53、更新 T46/T47 反映新行为；真实程序逐像素确认标题栏取色与行号列宽。**已本地打包 v1.8.4**。
- [x] 第十五轮 四项 + 一处连带修复：① **打字进编辑 + 中文输入法**（引入 key-sink，见 §6.5；期间走过一次弯路：先只做"不 preventDefault、切焦点"，英文好了但输入法首字母仍被当普通字符，因为焦点切换发生在按键之后 → 改为焦点常驻可编辑元素）② **冻结（只读）**开关 ③ 行号右键「注释该行」 ④ 筛选框右键菜单 + X 清除按钮。连带：key-sink 是 TEXTAREA，把 `find.js` 的 Ctrl+F 守卫打回了 WebView2 默认查找（"系统搜索框"）→ 抽出 `dom.js` 集中判定并统一排除 key-sink，顺带修好"筛选框里 Ctrl+F 被夺焦点"的老问题。**已本地打包 v1.8.4**。
- 教训（测试脚手架，第二次踩）：同一逻辑格两次合成 mousedown 间隔 <400ms 会命中"双击进入编辑"分支、不更新选区，误判为产品 bug；合成测试必须给足间隔并 dispatch mouseup。
